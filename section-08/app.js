// core modules
const path = require('path');
// tp modules
const express = require('express');

// init
const app = express();
app.set('view engine', 'ejs');

// import routes

// run
app.listen(3000);